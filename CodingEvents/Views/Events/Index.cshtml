<p>
    <a asp-controller="events" asp-action="add">Add Event</a>
</p>
<h1>Events</h1>

@if (ViewBag.events.Count == 0)
{
    <p>No events to display.</p>
}
else 
{ 

    <table>
        <tr>
            <th>Event Name</th>
            <th>Description</th>
        </tr>
        @foreach (KeyValuePair<string, string> meeting in ViewBag.Events)
         {
            <tr>
                <td>@meeting.Key</td>
                <td>@meeting.Value</td>
            </tr> 
         }
    </table>

}
